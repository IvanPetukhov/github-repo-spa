import * as React from 'react';
import './header.css';

export const Header = () => {
    return (
        <div className={ 'header' }>
            Публичные репозитории GitHub
        </div>
    );
};